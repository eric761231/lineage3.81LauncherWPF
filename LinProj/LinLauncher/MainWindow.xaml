<Window x:Class="LinLauncher.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        xmlns:local="clr-namespace:LinLauncher"
        xmlns:vm="clr-namespace:LinLauncher.ViewModels"
        Title="Lineage 3.81 Launcher" 
        Height="{Binding Config.Height}" Width="{Binding Config.Width}"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen">
    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>
    <Border CornerRadius="10" Background="#121212" BorderBrush="#333" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions><RowDefinition Height="40"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
            <Grid Grid.Row="0" Background="#1a1a1a" MouseDown="TitleBar_MouseDown">
                <TextBlock Text="LINEAGE 3.81 PREMIUM LAUNCHER" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="15,0,0,0" Foreground="#888" FontSize="12" FontWeight="Bold"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,10,0">
                    <Button Content="−" Width="30" Height="25" Background="Transparent" Foreground="White" BorderThickness="0" Click="Minimize_Click"/>
                    <Button Content="✕" Width="30" Height="25" Background="Transparent" Foreground="White" BorderThickness="0" Click="Close_Click"/>
                </StackPanel>
            </Grid>
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions><ColumnDefinition Width="2*"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                <Border Grid.Column="0" Margin="10" CornerRadius="5" ClipToBounds="True">
                    <wv2:WebView2 Name="webView" Source="{Binding Config.Web}"/>
                </Border>
                <Grid Grid.Column="1" Margin="10,10,20,10">
                    <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
                    <TextBlock Text="伺服器列表" Foreground="#7232f2" FontWeight="Bold" Margin="0,0,0,10"/>
                    <ListBox Grid.Row="1" ItemsSource="{Binding Servers}" SelectedItem="{Binding SelectedServer}" Background="#1a1a1a" BorderThickness="1" BorderBrush="#333" Foreground="White">
                        <ListBox.ItemTemplate><DataTemplate><Grid Margin="5"><TextBlock Text="{Binding Name}" FontSize="14" FontWeight="SemiBold"/></Grid></DataTemplate></ListBox.ItemTemplate>
                    </ListBox>
                    <StackPanel Grid.Row="2" Margin="0,20,0,0">
                        <Button Command="{Binding StartCommand}" Height="50" Content="遊戲開始" Background="#7232f2" FontSize="18" FontWeight="Bold" Foreground="White" Cursor="Hand"/>
                    </StackPanel>
                </Grid>
            </Grid>
            <Grid Grid.Row="2" Height="30" Background="#1a1a1a" Margin="0,0,0,10">
                <ProgressBar Value="{Binding OverallProgress}" Height="2" VerticalAlignment="Top" Background="Transparent" BorderThickness="0" Foreground="#7232f2"/>
                <TextBlock Text="{Binding StatusText}" VerticalAlignment="Center" Margin="15,0,0,0" Foreground="#666" FontSize="11"/>
            </Grid>
        </Grid>
    </Border>
</Window>

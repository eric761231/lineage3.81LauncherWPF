# 2026-09-16

1.整理並推送先前累積的修改到 GitHub，同時把 Jules（另一位協作者）在獨立分支上做的程式碼註解/格式整理合併回主分支，過程中處理掉 2 個檔案的合併衝突，合併後重新建置 Encoder／登入器／LauncherDll 三個專案確認都正常
2.部署腳本（deploy.ps1）新增自動建置功能，執行部署時會自動找電腦上的 Visual Studio 幫 LauncherDll、LineageIme 這兩個原生 DLL 重新建置，不用再手動先建置好才能部署
3.修正登入器 DLL 熱更新機制的兩個問題：檔名統一改用 .bin（原本用 .new，跟既有慣例不一致）；改用完整自封裝方式重新編譯登入器，避免玩家電腦沒裝 .NET 還要另外提示安裝
4.查證另一個 GM 指令封包（S_SkillHaste，技能加速/減速狀態圖示）在客戶端的處理位址，過程中發現這個封包真正的 opcode 編號是 255，先前參考文件寫的 149 是錯的；花了不少時間試著用反組譯找出正確的客戶端處理位址，兩次嘗試都沒找到，先停下來把已知線索、排除掉的路都寫進文件，暫時裝上去測試用的 hook 也已經清除
5.處理登入器技能圖示美術素材，幫忙切割、去背、加上跟現有圖示風格一致的柔邊效果，統一成 64x64 尺寸
